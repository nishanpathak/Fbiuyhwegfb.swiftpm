import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "soccerball")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Premier League")
        }
    }
}
